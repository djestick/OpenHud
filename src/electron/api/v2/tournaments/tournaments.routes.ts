import { Router } from "express";

export const tournmentRoutes = Router();

// To be added

/* ================== GETs ===================== */
tournmentRoutes.get("/", (req, res) => {
  res.send(null);
});

/* ================== POSTs ===================== */

/* ================== PUTs ===================== */
